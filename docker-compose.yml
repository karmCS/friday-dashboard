services:
  friday-dashboard:
    build: .
    container_name: friday-dashboard
    restart: unless-stopped
    # Secrets live in the on-box .env (chmod 600, gitignored) — never inline here.
    env_file: .env
    environment:
      NODE_ENV: production
      PORT: "3000"
    volumes:
      # SQLite DB (/data/friday.db) + uploaded photos (/data/photos)
      - ./data:/data
    ports:
      # Bind to loopback only — reached via the cloudflared tunnel (host :3000 is portfolio).
      - "127.0.0.1:3003:3000"
