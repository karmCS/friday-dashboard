# dependencies
/node_modules

# next.js
/.next
/out

# production build
/build

# env files — NEVER commit secrets (public repo)
.env
.env.local
.env*.local

# local SQLite data + uploaded photos
/data
*.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# misc
.DS_Store
*.pem
next-env.d.ts
*.tsbuildinfo

# local design reference (canonical artifact lives in Claude Design)
/design

# local scratch SQLite (dev FRIDAY_DB_PATH); .db-wal/.db-shm don't match *.db
/tmp-data

# local Claude Code agent state (worktrees, settings, memory)
/.claude

# crash artifacts
*.stackdump
